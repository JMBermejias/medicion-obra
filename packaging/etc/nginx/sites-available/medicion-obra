# Medicion Obra - sitio nginx
# Copyright (C) 2026 JMBernabeu - GPL-3.0-or-later
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    root /var/www/medicion-obra;
    index mediotec.html;

    location / {
        # Autenticacion basica solo desde fuera (protege acceso desde internet)
        set $block_auth "";
        if ($remote_addr = 127.0.0.1) { set $block_auth "local"; }
        if ($remote_addr = ::1) { set $block_auth "local"; }
        if ($block_auth = "") {
            auth_basic "Medicion Obra";
            auth_basic_user_file /etc/medicion-obra/.htpasswd;
        }
        try_files $uri $uri/ /mediotec.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
